# Создание проектов C# в командной строке Windows
Мы уже начинаем делать практические упражнения. Все упражнения будут иметь сквозную нумерацию, но будут разбиты по эпизодам,
к которым они относятся. То есть каждое упражнение будет иметь уникальный номер. Все упражнения вы сможете найти на [GitHub](https://github.com/it-0n/it0nCS).

Очень рекомендую делать каждое упражнение ручками, а не копировать код с [GitHub](https://github.com/it-0n/it0nCS). К коду из репозитория стоит прибегать
если только у вас, по какой-то причине, что-то не заработало. Но это только рекомендация. Конечно вы можете поступать
как вам удобнее.

>В первых упражнениях я буду стараться разжевывать все максимально подробно. Надеюсь получиться не очень занудно. Этот
>курс рассчитан на людей начинающих с полного, тотального нуля 😊. Если вы не относитесь к таковым, то просто пропускайте
>те объяснения которые вам кажутся занудными и излишними.
{style="note"}

## Упражнение 0001. Создание структуры папок, для упражнений и первого проекта
Структура будет очень простая. Создайте в корне какого-нибудь диска, например **D**, каталог it0nCS. И далее, уже в этом
каталоге мы будем создавать подкаталоги episode01, episode01 и т.д. А в каталогах эпизодов уже будут каталоги упражнений:
ex0001, ex0002 и т.д.

Если у вас есть только диск С, то создайте каталог на нем, но вообще, на мой взгляд, это плохой тон держать систему и проекты на одном диске.

>Хотя если говорить более точно, то как правило диски в Windows это на самом деле разделы, на одном физическом диске. Вообще
>Микрософт сильно подзапутало людей назвав разделы диска, дисками C, D, E и т.д. Опять же раздел может занимать целый диск,
>но при этом это все же раздел, а не диск. Если вы не поняли о чём я, то просто забейте. 😊
{style="note"}

Запустите командную строку. Как это сделать вы уже должны знать из [предыдущей статьи](Install-NET-SDK-8-in-Windows.md#start-command-prompt).

Как правило, командная строка запускается в домашнем каталоге пользователя. Мы же хотим создать каталог it0nCS в корне диска
**C** или **D**.

Если у вас есть только диск **С**, то выполните команду `cd c:\`, чтобы оказаться в корне диска **С**.

Если у вас есть диск D, то выполните команду `cd /d d:`

Далее все команд будут одинаковыми.

Создадим каталог it0nCS командой `mkdir it0nCS`

перейдем в него командой `cd it0nCS`

Далее создадим каталог episode01 командой `mkdir episode01`

и перейдем в него командой `cd episode01`

И, наконец, создадим наш первый проект на C# командой: 

`dotnet new console -o ex0001_hello_world`

Данная команда, создаст каталог ex0001_hello_world, а в нем создаст файлы проекта C#.

Перейдем каталог проекта командой `cd ex0001_hello_world`

И посмотрим содержимое каталога командой `dir`

![Создание проекта C# в командной строке Windows](ex0001_creation.png){ border-effect="line" thumbnail="true" width="700"}

Рассмотри подробнее команду `dotnet new console -o ex0001_hello_world`.

Часть `dotnet new` создает новый проект, далее указывается тип проекта `console`, то есть проект, который предназначен
для работы в командной строке, консольное приложение. Ключ `-o` (output) задает расположение для размещения созданных
выходных данных (файлов проекта), а так же название проекта, совпадающее с названием каталога в котором
размещен проект.

Таким образом мы создали консольный проект C#, который включает в себя два файла и одну папку:
- Program.cs - это как раз и есть код нашего приложения
- ex0001_hello_world.csproj - файл описывающий наш проект
- папка obj - в ней находятся объектные(скомпилированные в объектный код) файлы проекта

>После того как вы запустите программу, появиться еще и папка **bin**, в которой будет находиться готовое к запуску приложение.
>Если не все понятно про папки, то просто примите это к сведению и пропустите. Более подробно рассмотрим эту тему чуть позже.
{style="note"}

Далее запустим нашу программу командой `dotnet run`.

В результате мы увидим вывод нашей программы: `Hello, World!`

<format style="bold" color="Coral">Поздравляю! Вы создали и запустили свою первую программу на C#! При этом не написав ни строчки кода 😊</format>

Давайте поймем как это получилось. Тут все дело в том, что в .NET SDK существуют так называемые шаблоны приложений. Это,
если можно так сказать, заготовки типовых приложений например, как только что мы сделали для консоли. Так же есть, ещё
много разных шаблонов идущих в стандартной поставке .NET SDK, например для создания веб приложения, или библиотеки и т.п.
Вы так же можете [создать сами шаблон приложения](https://learn.microsoft.com/ru-ru/dotnet/core/tutorials/cli-templates-create-project-template)
или скачать готовые, разработанные другими людьми, [например эти](https://github.com/Dotnet-Boxed/Templates).

{id=template_list}Посмотреть доступные шаблоны приложений идущие в стандартной поставке .NET SDK можно командой:

`dotnet new list`

![Посмотреть список шаблонов dotnet](dotnetnewlist.png){border-effect="line" thumbnail="true" width="700"}

Теперь давайте посмотрим на код(текст) нашей программы **Program.cs**. Откройте её в любом текстовом редакторе например
в Блокноте(notepd). Вам нужно открыть файл **Program.cs**, находящийся в каталоге **D:\it0nCS\episode01\ex0001_hello_world**

![Открыть файл Program.CS в Блокноте](openProgramCS.png){ order-effect="line" thumbnail="true" width="700"}

Вы должны увидеть текст(код), программы:

![Посмотреть список шаблонов dotnet](notepadProgramCS.png){border-effect="line"}

На этом пока все. Закройте Блокнот, или тот редактор в котором вы открыли программу. Во втором упражнении мы создадим
еще одну программу и уже слегка изменим ее.

>Стрелочками я подсветил, кодировку и символ конца строки, которые показывает редактор notepad.exe.
>Обратите внимание, что кодировка должна быть UTF-8.
{style="note"}

```c#
// See https://aka.ms/new-console-template for more information
Console.WriteLine("Hello, World!");

```
В программе три строки(последняя третья пустая). Первая строка это комментарий, то есть простой текст, который не компилируется
в исполняемый код, а вторая строка это как раз таки уже код программы, то есть инструкция компьютеру вывести в консоли фразу 
**Hello World!**
Если вы смотрели внимательно, то должны были заметить что в команде

```c#
Console.WriteLine("Hello, World!");
```
присутствуют двойные кавычки, но они не были выведены в консоли. Почему это так мы рассмотрим позже.

Теперь заглянем в каталог bin, нашего проекта, там будет папка Debug, а в ней папка net8.0. Вот тут и располагается готовое
к запуску наше приложение.

![Дерево папок и файлы приложения](ex0001_tree_explorer.png){border-effect="line" thumbnail="true" width="700"}

На данный момент в консоли вы должны находиться в папке **ex0001_hello_worl**. Давайте перейдем в папку `bin\Debug\net8.0`
дав команду `cd bin\Debug\net8.0\`

Затем посмотрим ее содержимое командой `dir` и далее запустим нашу программу `ex0001_hello_world.exe`

![Запуск скомпилированного приложения](ex0001_run_exe.png){border-effect="line" thumbnail="true" width="700"}

Как вы можете видеть в папке, кроме исполняемого файла `ex0001_hello_world.exe` есть еще несколько других файлов. Чтобы
эта программа запустилась на другом компьютере необходимы следующие условия:

- на компьютере должен быть установлен .NET Runtime такой же или выше версии, чем тот под которым собиралась программа, в нашем случае .NET 8 Runtime или выше
- на компьютер необходимо передать либо всё содержимое папки .net8.0, либо как минимум файлы с расширениями .dll и .exe

Назначение всех этих файлов мы рассмотрим как-нибудь позже. Пока достаточно знать, где можно найти готовую к выполнению
программу и как ее запустить.

## Упражнение 0002. Создаем консольное приложение C# без операторов верхнего уровня
Что это еще за [операторы верхнего уровня](https://learn.microsoft.com/ru-ru/dotnet/csharp/fundamentals/program-structure/top-level-statements)?

До C# 9, вы не могли так просто написать программу, как это было сделано в предыдущем упражнении. Честно говоря, мне самому
не очень привычно и удобно ими пользоваться. Давайте посмотрим как выглядела раньше простая программа из предыдущего 
упражнения без этих операторов верхнего уровня.

Если вы дадите в консоли команду `dotnet new console -h`, чтобы посмотреть справку по возможным ключам для создания
консольного приложения (для шаблона console), то увидите следующее:

![dotnet new console -h](dotnetnewconsole-h.png){ border-effect="line" thumbnail="true" width="700"}

Кстати там много и других интересных и полезных ключей, некоторые из которых мы рассмотрим в других упражнениях.

И так, перейдите в каталог D:\it0nCS\episode01, дав в консоли команду 

`cd /d D:\it0nCS\episode01`

Затем создайте каталог для нашего нового проекта командой 

`mkdir ex0002_main_hello_cs`

Перейдите в созданный каталог командой 

`cd ex0002_main_hello_cs`

Создайте новое приложение командой 

`dotnet new console --use-program-main`

Посмотрите каталог командой `dir`

![dotnet new console --use-program-main](dotnetnewconsole--use-program-main.png){border-effect="line" thumbnail="true" width="700"}

Обратите внимание, что в команде создания приложения в этом упражнении нет ключа `-o`, который создает папку для приложения.
Я опустил этот ключ, так как мы создали паку для приложения сами, затем зашли в нее, и уже там создали приложение.
И что приложение было дано такое же название, как и название папки в которой оно было создано. Название приложения определяется
именем файла с расширением .csproj, в данном случае приложение имеет имя **ex0002_main_hello_cs**.

>Проанализируйте и сравните команды, использованные для создания приложений в первом и втором упражнениях. Постарайтесь понять разницу.
>Хотя сейчас, возможно, вам это будет ещё сложновато, но попробовать стоит.
> {style="note"}

Давайте посмотрим, на код в файле Program.cs. 

![Вид в проводнике Program.cs](ex0002-01.png){ border-effect="line" thumbnail="true" width="700"}

Откройте его в каком-нибудь текстовом редакторе. В этот раз я открою файл в редакторе SublimeText, который делает приятную
подсветку синтаксиса (команд языка).

>У вас для файла Program.cs, может быть, и скорее всего и будет, другая иконка. Это не страшно. Пока забейте на это.
{style="warning"}

>Текстовых редакторов существует великое множество. Один из наиболее любимых у пользователей Windows - это [Notepad++](https://notepad-plus-plus.org/).
>Он бесплатный, и обладает обширным функционалом. Так же в нём есть и подсветка синтаксиса многих языков программирования.
>Установите его себе и пользуйтесь на здоровье.
{style="note"}

![Program.cs в редакторе SublimeText](use-program-main.png){border-effect="line"}

Как видим кода стало куда больше. И на данный момент вам вообще может быть не понятно, что все это значит. И скорее всего
все мои последующие объяснения будут вам не очень поняты, если вы начинаете изучать программирование с нуля. Если вам что-то
будет не понятно, то расстраиваться точно не стоит. Вы и ваш мозг ни когда с этим не сталкивались. Ему надо строить новые 
нейронные связи и делать кучу другой работы которую он делать не хочет. Мозг - это ленивая скотина, которая любит ходить
проторенными тропами. Но мы не ищем лёгких путей.! Поэтому даже если что-то и будет не понятно, или даже все будет не понятно,
то просто продолжайте ПРАКТИКОВАТЬСЯ. Практика ведет к совершенству. И через практику вы начнете понимать, то что делаете.

По существу операторы верхнего уровня делают скрытие всего того кода который вы видите сейчас вокруг единственной команды
вывода в консоль радостного приветствия мира `Console.WriteLine("Hello, World!");`. И на самом деле скрытые команды
присутствуют и даже в этом коде. Просто вы их не видите 😊, но они подразумеваются. Это команды подключения стандартных
библиотек.

На самом деле весь этот код который вы видите сейчас присутствует и в первой программе, только вы его не видите 😊. Это как
суслик которого ни кто не видит, а он есть. Посмотрите поясняющее видео.

<video src="https://youtu.be/nVyk0KMHAYA" width="700" preview-src="suslik.png"/>

Ну ладно хватит болтать. Погнали дальше. Построчно рассмотрим получившуюся программу.

В первой строке объявляется **namespace** (пространство имен). Это такая штука которая позволяет группировать классы. Как раз ключевое слово
**class** вы видите на третьей строке программы. Класс это можно сказать основной строительный блок программы. **Все инструкции
(так скажем команды программы) могут находиться только внутри классов.** Да в программе может быть не один, а множество классов.
Вернее сказать реальная программа всегда состоит из десятков, сотен и даже тысяч классов.

Каждый класс может содержать множество инструкций (например вывод в консоль сообщений) для исполнения процессором.

У каждого класса в пределах одного пространства имен (**namespace**) к которому он относится должно быть уникальное имя.
Исключение составляют только анонимные классы, ну на то они и анонимные (с ними мы тоже познакомимся, но позже).

Например, наша программа имеет единственный класс с именем **Program**. Фигурные скобки (открывающая и закрывающая) показывают
границы класса. Все операторы, находящиеся внутри этих скобок, принадлежат этому классу. Операторы вы можете рассматривать
как кирпичики, которые находятся внутри блока (класса).

В данном случае внутри класса Program, есть статический (**static**) метод с именем **Main**, который является точкой входа в программу,
то есть местом от куда начинается выполнение программы. Ключевое слово **void** означает что метод **Main** не возвращает ни какого значения.

Я говорил, что не все будет понятно. Пока просто читайте и запоминайте. Ваш мозг потом все сам систематизирует, разложит по 
полочкам, и в один прекрасный момент вас озарит понимание 😊 и все станет ясно как божий день.

Метод **Main** принимает входящие параметры в виде массива строк **string**, которые помещаются в массив с названием **args**.
Параметры метода находятся внутри круглых скобок. Метод может быть без параметров.
Каждый метод имеет свои границы, которы так же обозначаются фигурными скобками.

Внутри метода **Main** есть одна единственная команда или инструкция`Console.WriteLine("Hello, World!");`, которая выводит в 
консоль сообщение `Hello, World!`.

Каждая инструкция (statement) должна заканчиваться точкой с запятой **;** которая означает конец инструкции.

Надеюсь получилось не очень заумно и более менее понятно. Теперь чтобы на практике все это дело закрепить, давайте немного
поменяем нашу программу и заменим текст **Hello, World!** на свой.

Далее нам надо будет редактировать текст программы в текстовом редакторе. Привести программу к следующему виду означает, 
что вам надо отредактировать код(текст) программы так, чтобы он соответствовал тому что вы видите в задании.

>Не забывайте сохранять текст(код) программы, перед тем как вы будете ее компилировать или запускать.
{style="warning"}

Приведите программу к следующему виду:

![Program.cs в редакторе SublimeText](ex0002_02.png){border-effect="line"}

Обратите внимание, что что используются разные методы для вывода в консоль: `Console.Write` и `Console.WriteLine`. Они отличаются
только тем, что `Console.WriteLine` делает перевод на новую строку, a `Console.Write` оставляет курсор на прежней строке.

Давайте запустим нашу программу:

![1 запуск измененной программы ex0002](ex002_run01.png){border-effect="line"}

Постарайтесь понять, почему три оператора вывода в консоль все вывели в одну строку. И кроме того нет пробелов между предложениями.

Теперь вам будет первое самостоятельное задание. Исправьте эту программу, так чтобы она выводила текст следующим образом:

![2 запуск измененной программы ex0002](ex002_run02.png){border-effect="line"}

Обратите внимание, что теперь есть пробелы между предложениями.

Вторым заданием будет изменить программу так, чтобы она сделала следующий вывод:

![3 запуск измененной программы ex0002](ex002_run03.png){border-effect="line"}

В программировании у каждой задачи может быть несколько решений. Для вас сейчас главное добиться такого же вывода, как показано
на этих двух последних скриншотах. А как вы это сделаете это ваше дело. Удачи!

## Упражнение 0003. Работа в приложении с аргументами переданными при запуске приложения
Сейчас вы уже умеете создавать консольные приложения из шаблона. Поэтому создайте приложение с названием **ex0003_args**.
Приложение должно находится в папке **ex0003_args**, которая должна находится в папке **episode01**. В результате создания
приложения у вас должна получиться такая структура папок:

![Упражнение 0003. Структура папок](ex0003_folders.png){border-effect="line"}

В этом упражнении мы уже будем передавать в нашу программу некоторые данные через аргументы при запуске в командной строке.

Откройте файл Program.cs, находящийся в директории ex0003_args и приведите его к следующему виду:

![ex0003 Program.cs](ex0003_ProgramCS.png){border-effect="line" thumbnail="true" width="700"}

Запустите программу:

![ex0003 Program.cs run](ex0003_run01.png){border-effect="line"}

После запуска программы, как вы уже знаете, будет создана папка bin с вложенными папками Debug и далее net8.0.

Перейдите в папку bin\Debug\net8.0 командой:

`cd bin\Debug\net8.0\`

Дайте команду `dir`, чтобы посмотреть содержимое каталога и запустите нашу программу с одним и двумя аргументами:

![ex0003 Program.cs run](ex0003_run02.png){border-effect="line"}

Эта программа стала немного сложнее. Появился условный оператор **if**. А так же работа с массивом строк args.

Давайте разбираться, что же происходит в нашей программе и как она работает.

При запуске программы операционная система всегда передает в нее аргументы, которые идут через пробел после имени программы.
Этими аргументами являются строки(слова). В примере на скриншоте в первом запуске это слово **Первый**, во втором запуске
это два слова **Первый** и **Второй**. То есть при первом запуске был передан один аргумент, при втором два аргумента. О 
чём нам собственно и сообщила программа.

И так, в первом запуске мы передали один аргумент, это была строка **Первый**. Во втором запуске мы передали два аргумента,
то есть строки **Первый** и **Второй**. Операционная система передала их в нашу программу, а программа приняла их в строковый
массив **args**. Пока мы еще не изучали массивы, поэтому воспринимайте массив, как контейнер для объектов, в данном случае как
контейнер для строк. И это такой контейнер с пронумерованными полочками. Нумерация начинается с нуля.

Оператор вывода на консоль в 7 строке сообщает сколько было передано всего аргументов. Количество аргументов берется из 
свойства **Length** объекта **args**. 

```c#
Console.WriteLine($"Было передано аргументов {args.Length}");
```

Чтобы было понятнее можно сказать, что все строки(слова) которые вы вводите после названия
программы в командной строке попадают в "контейнер" с названием **args**. И там они хранятся. Их так же можно от туда
достать, так как хранятся они все под номерами, начиная с нуля. Но все же лучше сразу знать правильные термины. То что
я обозвал контейнером называется массивом, а данные которые в нем находятся имеют индекс, который начинается с нуля. Чтобы
обратиться к первому элементу в массиве **args** нужно указать его индекс 0(нуль). Таким образом обращение к первому 
элементу массива **args** выглядит так: `args[0]`. Помним что нумерация элементов массива начинается с нуля.

Не беспокойтесь если не всё сейчас понятно. Со временем во всём разберётесь.

В седьмой строке мы видим условный оператор **if**, который проверяет, были ли переданы аргументы при запуске нашей программы 
(то есть их количество больше нуля). И если аргументы были переданы, то выполняется строка 9, которая выводит первый переданный
аргумент. Помните что нумерация в массиве начинается с нуля. Поэтому первый переданный аргумент помещается, так скажем, в ячейку
с номером нуль. А ещё вернее сказать имеет индекс 0(нуль).

```c#
if(args.Length > 0 )
    Console.WriteLine($"Первый переданный аргумент: {args[0]}");
```

Чтобы лучше понять работу программы, позапускайте её с разными аргументами и с разным их количеством. Например:

![ex0003 Program.cs run](ex0003_run03.png){border-effect="line"}

Посмотрите внимательно на каждый вывод программы при каждом запуске. Проанализируйте каждый вывод программы. Постарайтесь
понять почему он именно такой. Сделайте свои эксперименты с запуском программы.

А теперь еще разок вспомним об операторах верхнего уровня из второго упражнения. Помните я вам говорил что есть еще операторы
которых вы не видите? Да в программировании, как и в жизни полного того что мы не видим, но оно есть. Короче кругом одни 
невидимые суслики 😊

{id=RiderDefaultNamespace}Посмотрите на скриншот ниже. Среда разработки JetBrains Rider может показать подключенные по умолчанию библиотеки в
нашей программе. Они подключаются автоматически. Библиотеки в программирование это готовые наборы программ, функций и 
объектов, которые вы можете использовать в своей программе. Например, мы пользуемся методом **WriteLine()** из статического
класса **Console**, находящегося в базовой библиотеке **System**. Базовая в данном случае подразумевается что это часть платформы .NET.
Вы не писали код метода **WriteLine()**, а просто подключили, вернее сказать за вас .NET подключил библиотеку **System** к вашей
программе и вы просто пользуетесь уже ГОТОВЫМ КОДОМ, который написали другие добрые программисты. Рассматривайте библиотеки
как готовые маленькие программы, которые вы можете использовать в своей программе. Как правило, библиотека включает в себя
достаточно большое количество таких маленьких программ (на то она и библиотека). Библиотеки имеют, если так можно сказать,
специализацию. То есть объединяют в себе маленькие программы, которые решают задачи в какой-либо одной предметной области.
Например, есть математические библиотеки или библиотеки работы с сетью и т.п.

![ex0003 Program.cs](ex0003_ProgramCS01.png){border-effect="line" thumbnail="true" width="700"}

Для справочки и пущего понимания приведу лишь небольшую часть кода, который скрывается за, казалось бы, простым методом
**WriteLine()**. Это пришлось очень углубиться в недры библиотеки **System**. Ясен пень что сейчас вы не поймете этого кода.
Но зато будете знать, что кто-то для вас потрудился и написал, то чем вы сейчас пользуетесь в своей программе.

![WriteLine lib](ex0003_write.png){border-effect="line" thumbnail="true" width="700"}

### Практическое задание

Пришло время практического задания. Создайте приложение ex0003pt01, в каталоге episode01 без операторов верхнего уровня.
Как это сделать вы уже знаете. Структура папок должна выглядеть так:

![ex0003pt01 folders Program.cs](ex0003_pt01.png){border-effect="line" thumbnail="true" width="700"}

>pt - это сокращение от слов practical task (практическая задача).
> {style="note"}

В принципе, именовать приложения вы можете как вашей душе угодно. Просто с названиями из курса все упражнения
и практические задачи будут на github. И вам будет проще ориентироваться.

За пример можете взять приложение ex0003_args. Ваше приложение **ex0003pt01** должно при получении одного или более
аргументов командной строки, выводить приветствие первого аргумента. Если аргументов при запуске программы нет, то программа
не должна ни чего выводить. Смотрите пример того как должно работать приложение **ex0003pt01** на скриншоте ниже:

![ex0003pt01 Program.cs](ex0003pt01.png){border-effect="line" thumbnail="true" width="700"}

Обратите внимание на предпоследний запуск. Программа была запущена без аргументов.

>Подсказка. По существу это почти та же программа, что мы делали в 3 упражнении. Просто Вам надо одну строку вообще
>удалить, а одну строку изменить. Строку с оператором if вообще не трогать, оставить как есть.
>Считайте я уже вам рассказал как надо выполнить задание. Вам надо лишь чуток подумать и всё сделать. Удачи, падаван!
> Да пребудет с тобой Сила!
{style="note"}

Если у вас не получиться сделать данное задание сейчас, просто пропустите его. Сделаете позже, когда уже будете знать чуть
больше. Но вообще знаний и практики, которые вы тут уже получили, вам должно хватить, чтобы сделать это задание.

## Упражнение 0004. Интерактивный ввод данных в консольном приложении.
Падаван, теперь ты уже должен умеешь на раз создавать консольные приложения в командной строке. Поэтому далее задачи будут
выглядеть уже так:
- Создай консольное приложение С ОПЕРАТОРАМИ ВЕРХНЕГО УРОВНЯ в папке episode01
- Приложение должно называться ex0004_interactive
- Перейди в папку ex0004_interactive и отредактируй код программы приведя его к следующему виду

![ex0004 Program.cs](ex0004_ProgramCS.png){border-effect="line"}

- Запустите программу несколько раз и введите разные имена. Например:

![ex0004 Program.cs run](ex0004_ProgramCSrun.png){border-effect="line"}

Давайте пробежимся по коду...

В первой строке был оставлен автоматически сгенерированный комментарий.

Магию второй строки объяснять пока не буду. Скажу лишь то, что без нее введенная в качестве ответа кириллица не будет 
потом правильно отображаться в приветствии.

Третья строка выводит запрос имени и оставляет курсор на той же строке. Далее ожидается ввод от пользователя.

В четвертой строке происходит получение данных введенных пользователем и помещение их в строковую переменную **padavanName**.

В пятой строке мы приветствуем по имени, извлекая введенное имя из переменной **padavanName**.

Рассматривайте переменные как именованные ячейки памяти куда можно записать данные и извлечь их от туда.

Первое задание к этому упражнению будет простое. Закомментируйте вторую строку и запустите программу заново. В первом запуске
введите любое имя или вообще любые символы на латинице. Во втором запуске на кириллице.

>Если не знаете как закомментировать код, то посмотрите на первую строку. Там всё есть ;)
{style="note"}

Если вы все сделали правильно, то вместо вывода на кириллице будут пустые символы. Это потому что вы отключили силу второй
строки. 😊 Включите её обратно 😊. Да пребудет с вами Сила!

Второе задание:
- Создай консольное приложение С ОПЕРАТОРАМИ ВЕРХНЕГО УРОВНЯ в папке episode01
- Приложение должно называться ex0004pt1
- Приложение должно спрашивать имя у пользователя и приветствовать его как показано на скриншоте ниже:

![ex0004pt1 run](ex0004pt1run.png){border-effect="line"}

Если у вас возникнут трудности с выполнением этого задания, то вы так же можете его пропустить. Сделаете позже, когда 
будете знать больше.